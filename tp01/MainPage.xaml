<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="tp01.MainPage"
    BackgroundColor="Black"
    Title="Aplicação Teste">

    <ContentPage.Content>
        <Grid Padding="20" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Text="Aplicação Teste"
                   HorizontalOptions="Center"
                   FontAttributes="Bold"
                   FontSize="22"
                   TextColor="White"
                   Margin="0,8,0,16"/>

            <StackLayout Grid.Row="1" Spacing="12" HorizontalOptions="FillAndExpand">
                <Grid ColumnDefinitions="Auto, *" VerticalOptions="Center">
                    <Label Text="ID:" VerticalOptions="Center" TextColor="White" Margin="0,0,8,0"/>
                    <Frame Grid.Column="1" Padding="4" BorderColor="White" BackgroundColor="Transparent" HasShadow="False" CornerRadius="4">
                        <Entry x:Name="IdEntry" Placeholder="Digite seu ID" TextColor="White" PlaceholderColor="LightGray" />
                    </Frame>
                </Grid>

                <Grid ColumnDefinitions="Auto, *" VerticalOptions="Center">
                    <Label Text="Pass" VerticalOptions="Center" TextColor="White" Margin="0,0,8,0"/>
                    <Frame Grid.Column="1" Padding="4" BorderColor="White" BackgroundColor="Transparent" HasShadow="False" CornerRadius="4">
                        <Entry x:Name="PassEntry" Placeholder="Digite sua senha" IsPassword="True" TextColor="White" PlaceholderColor="LightGray" />
                    </Frame>
                </Grid>
            </StackLayout>

            <StackLayout Grid.Row="2" Spacing="12" Margin="0,24,0,0" HorizontalOptions="FillAndExpand">
                <Button x:Name="OkButton" Text="OK" Clicked="OkButton_Clicked"
                        BackgroundColor="#D3D3D3" TextColor="#8B0000" FontAttributes="Bold" HeightRequest="48"/>
                <Button x:Name="ClearButton" Text="Limpar" Clicked="ClearButton_Clicked"
                        BackgroundColor="#D3D3D3" TextColor="#8B0000" FontAttributes="Bold" HeightRequest="48"/>
                <Button x:Name="CreditsButton" Text="Créditos" Clicked="CreditsButton_Clicked"
                        BackgroundColor="#D3D3D3" TextColor="#8B0000" FontAttributes="Bold" HeightRequest="48"/>
            </StackLayout>

            <BoxView Grid.Row="4" HeightRequest="1" BackgroundColor="Transparent"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>
