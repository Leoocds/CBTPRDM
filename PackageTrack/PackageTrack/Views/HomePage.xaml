<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PackageTrack.ViewModels"
             x:Class="PackageTrack.Views.HomePage"
             Title="Rastreamento">

    <ContentPage.BindingContext>
        <vm:TrackViewModel />
    </ContentPage.BindingContext>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="18">

            <Label Text="Insira o código de rastreamento" FontSize="18" />

            <Entry x:Name="TrackingEntry"
                   Placeholder="Ex: BR123"
                   Text="{Binding TrackingCode}" />

            <Button Text="Buscar"
                    Clicked="OnSearchClicked"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

            <Label Text="Exemplos de uso" FontAttributes="Italic" />
            <Label Text="Código que termina com 'X' -> Em trânsito; caso contrário -> Entregue" />

            <Label Text="Leonardo de Lima Pedroso - CB3026655" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
