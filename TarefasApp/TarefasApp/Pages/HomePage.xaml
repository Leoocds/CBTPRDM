<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:TarefasApp.ViewModels"
             x:Class="TarefasApp.Pages.HomePage"
             Title="Minhas Tarefas">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Adicionar" Clicked="OnAddClicked" />
    </ContentPage.ToolbarItems>

    <VerticalStackLayout>

        <CollectionView ItemsSource="{Binding Tasks}"
                        SelectionMode="Single"
                        SelectionChanged="OnSelectionChanged"
                        HeightRequest="500">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="5" Padding="10" BorderColor="Gray" CornerRadius="8">
                        <VerticalStackLayout>
                            <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold"/>
                            <Label Text="{Binding Description}" FontSize="14" TextColor="Gray"/>
                            <Label Text="{Binding PriorityText}" FontSize="12" TextColor="Red"/>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="Ver Créditos"
                Clicked="OnCreditsClicked"
                HorizontalOptions="Center"
                Margin="0,20,0,0"/>
    </VerticalStackLayout>
</ContentPage>
